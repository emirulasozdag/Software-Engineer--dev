[project]
name = "adaptive-learning-backend"
version = "0.1.0"
description = "AI-powered adaptive curriculum backend (modular FastAPI)."
requires-python = ">=3.11"

[tool.ruff]
line-length = 100

[tool.pytest.ini_options]
addopts = "-q"
