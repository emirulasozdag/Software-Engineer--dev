from fastapi import APIRouter


api_router = APIRouter()

# Route modules exist in `app/api/routes/`, but are currently placeholders.
# When those modules define an `APIRouter` named `router`, include them here.
# Example:
# from app.api.routes import auth
# api_router.include_router(auth.router, prefix="/auth", tags=["auth"])

